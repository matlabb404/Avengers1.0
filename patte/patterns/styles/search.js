import { Stylesheet } from "react-native"

import { COLORS, FONT } from "../constants/theme"

const styles = Stylesheet.create({
    container: {
        width: "100%",
        color: COLORS.primary
    },
    
})
//your stylehseet legit justlike css